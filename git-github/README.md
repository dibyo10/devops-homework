# Git and GitHub exercise

This branch document was created as the third practice-branch commit.

The exercise demonstrates that `git commit -m` commits only staged changes, while `git commit -a -m` first stages modifications and deletions of already tracked files. It does not include new untracked files.
